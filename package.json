{
  "name": "manoj-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Production build pipeline for the Manoj S portfolio (static HTML/CSS/JS site).",
  "scripts": {
    "build": "node build.js",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "serve:dist": "npx serve dist -l 5500"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "fs-extra": "^11.2.0",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^4.1.1",
    "sharp": "^0.33.4",
    "svgo": "^3.3.2",
    "tailwindcss": "^3.4.19",
    "terser": "^5.31.0"
  }
}
